# Staging Environment Configuration
# Copy this file to .env.staging and fill in the values
# Usage: docker-compose -f docker-compose.staging.yml --env-file .env.staging up -d

# ===========================================
# DATABASE CONFIGURATION (PostgreSQL)
# ===========================================

# Database name
DB_NAME=crm_db

# Database user
DB_USER=postgres

# Database password - REQUIRED
# Generate a strong password (min 16 characters)
# Example: openssl rand -base64 24
DB_PASSWORD=CHANGE_ME_TO_STRONG_PASSWORD_MIN_16_CHARS

# ===========================================
# BACKEND CONFIGURATION
# ===========================================

# JWT Secret - REQUIRED
# Generate with: openssl rand -base64 32
JWT_SECRET=CHANGE_ME_TO_GENERATED_SECRET_32_CHARS

# JWT Token expiration
JWT_EXPIRES_IN=7d

# ===========================================
# FRONTEND CONFIGURATION
# ===========================================

# Backend API URL (update with your staging server URL)
API_URL=http://localhost:5000/api

# ===========================================
# EXTERNAL DATABASE (Optional)
# ===========================================
# If using external database service (e.g., Railway, Supabase, Neon):
# 1. Comment out the postgres service in docker-compose.staging.yml
# 2. Update DB_HOST with external database host
# 3. Update other DB_* variables as needed

# DB_HOST=your-external-db-host.railway.app
# DB_PORT=5432
